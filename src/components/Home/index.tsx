import { FC } from "react";
import { Link } from "react-router-dom";

const Home: FC = () => {
  return (
    <div
      style={{
        height: "150vh",
        overflow: "hidden",
      }}
    >
      <h1>Home</h1>
    </div>
  );
};

export default Home;
